@startuml
|Browser|
start
if (Diagram type: Class?) then (yes)
  :Enable layout selection;
else (no)
  :Disable layout selection;
endif
:Submit DSL source code;

|Server|
:Parse input & compute layout;
:Generate SVG representation;
:Another Action1;
:Another Action2;
if (Diagram type: Class?) then (yes)
  :Enable layout selection;
else (no)
  :Disable layout selection;
endif
|Browser|
:Render diagram;
stop
@enduml
