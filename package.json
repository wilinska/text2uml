{
  "name": "bazel-emscripten",
  "version": "1.0.0",
  "description": "Hello World to WebAssembly using Bazel and Emscripten",
  "main": "src/javascript/index.js",
  "license": "MIT",
  "scripts": {
    "build": "npm run build:wasm",
    "build:wasm": "bash src/javascript/scripts/build-wasm.sh && npm run build:copy",
    "build:copy": "bash src/javascript/scripts/build-copy.sh",
    "clean": "bazel clean --expunge && rm -rf src/javascript/bin/* && rm -rf src/javascript/dist/*",
    "prerollup": "rm -rf src/javascript/bin/* && rm -rf src/javascript/dist/* && npm run build:copy",
    "rollup": "rollup -c",
    "start": "node src/javascript/app.js"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.6",
    "rollup": "^2.58.0",
    "rollup-plugin-terser": "^7.0.2"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "marked": "^13.0.0",
    "pako": "^2.1.0",
    "svg-pan-zoom": "^3.6.1"
  }
}
